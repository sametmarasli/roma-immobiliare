version: 2

sources:
    - name: immobiliare
      database: roma-immobiliare-395210
      schema: dwh_test_dbt
      tables:
        - name: table_test_dbt

         # freshness:
           # error_after: {count: 6, period: hour}